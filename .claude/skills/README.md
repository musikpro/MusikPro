# Skills Claude Code

Les workflows métier restent centralisés dans `.agents/skills/`. Claude Code doit les lire depuis ces chemins pour éviter la duplication. Les commandes `.claude/commands/*` servent de raccourcis.
