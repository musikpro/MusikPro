import { desc } from "drizzle-orm";import { db } from "@/db";import { plans } from "@/db/schema";import { createPlan,togglePlan } from "./actions";
export default async function Page(){const rows=await db.select().from(plans).orderBy(desc(plans.createdAt));return <main className="shell"><h1>Plans</h1><form className="card" action={createPlan}><div className="grid"><label className="field">Nom<input name="name" required/></label><label className="field">Code<input name="code" required placeholder="pro"/></label><label className="field">Montant<input name="amount" type="number" min="0.01" step="0.01" required/></label><label className="field">Devise<select name="currency" defaultValue="XOF"><option>XOF</option><option>XAF</option><option>NGN</option><option>GHS</option><option>KES</option><option>USD</option><option>EUR</option></select></label><label className="field">Période<select name="interval"><option value="month">Mensuel</option><option value="year">Annuel</option></select></label><label className="field">Description<input name="description"/></label></div><button className="btn">Créer le plan</button></form><h2>Plans existants</h2><table className="table"><thead><tr><th>Plan</th><th>Prix</th><th>Période</th><th>État</th><th></th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.name}<br/><span className="muted">{p.code}</span></td><td>{p.amount.toLocaleString("fr-FR")} {p.currency}</td><td>{p.interval}</td><td>{p.active?"Actif":"Désactivé"}</td><td><form action={togglePlan}><input type="hidden" name="id" value={p.id}/><input type="hidden" name="active" value={String(p.active)}/><button className="btn secondary">{p.active?"Désactiver":"Activer"}</button></form></td></tr>)}</tbody></table></main>}
